# Внесок у проект

Дякуємо за бажання покращити **Аналіз броньювання — ЗП**! 🇺🇦

## 📜 Перед початком — CLA

Цей проект використовує **CLA (Contributor License Agreement)**.

Перш ніж надсилати Pull Request, ознайомтесь з [CLA.md](CLA.md) — це дозволяє Власнику проекту керувати ліцензуванням (включно з можливістю комерційної Pro-версії).

**Прийняття CLA = надсилання PR + підпис коміту.**

---

## 🐛 Як повідомити про помилку

1. Перевірте, чи **немає такої issue** в [списку issues](https://github.com/r0665629590-droid/bron-analysis/issues)
2. Створіть **нову Issue** з шаблоном:
   - **Що очікувалось:** …
   - **Що сталось:** …
   - **Кроки для відтворення:** 1) … 2) … 3) …
   - **Версія Python:** `python --version`
   - **Версія Windows:** 10 / 11
   - **Файл (якщо безпечно):** додати зразок xlsx з замаскованими даними

---

## ✨ Як запропонувати фічу

1. Створіть **Issue з міткою `enhancement`**
2. Опишіть **проблему**, яку фіча вирішує
3. Запропонуйте **рішення** і **альтернативи**
4. Зачекайте на обговорення перед PR — щоб не марно витрачати час

---

## 💻 Як надіслати Pull Request

### 1. Fork та клонування
```bash
git clone https://github.com/ВАШ_USER/bron-analysis.git
cd bron-analysis
```

### 2. Нова гілка
```bash
git checkout -b feature/коротка-назва
# або
git checkout -b fix/опис-багу
```

### 3. Налаштування середовища
```bash
# Windows
install.bat
```

### 4. Зробити зміни

Дотримуйтесь стилю коду:
- **4 пробіли** для відступу
- **PEP 8** базово (без фанатизму)
- **Українські** коментарі (узгоджується з рештою коду)
- **Логічно іменовані** функції та змінні

### 5. Тестування

Запустіть програму і перевірте:
- ✅ Відкривається без помилок
- ✅ Відкриває `sample_zp.xlsx` і `sample_dia.xlsx`
- ✅ Зберігається в Excel
- ✅ CSV експорт працює

### 6. Коміт **з підписом** (`-s` для CLA!)

```bash
git add .
git commit -s -m "fix: короткий опис змін"
```

> ⚠️ Опція `-s` додає рядок `Signed-off-by:` — це сигналізує **прийняття CLA**.

### 7. Push та PR

```bash
git push origin feature/коротка-назва
```

Створіть PR через GitHub UI з описом:
- **Що змінено**
- **Чому**
- **Як перевірити**
- Коментар: **«I agree to the CLA terms.»**

---

## 📋 Стандарти комітів

Формат повідомлення:
```
<тип>: <короткий опис> (не більше 70 символів)

<розгорнутий опис, якщо потрібно>

Signed-off-by: Ваше Імʼя <email@example.com>
```

Типи:
- `feat:` — нова функція
- `fix:` — виправлення багу
- `docs:` — лише документація
- `refactor:` — рефакторинг без зміни поведінки
- `test:` — тести
- `chore:` — рутина (оновлення залежностей, налаштування)

---

## 🚀 Що потрібно зараз

Поточні задачі (з пріоритетом):

- [ ] PDF-звіт через `reportlab`
- [ ] Графіки (matplotlib) — розподіл ЗП
- [ ] Drag & Drop xlsx (tkinterdnd2)
- [ ] Темна тема
- [ ] Історія по місяцях (SQLite)
- [ ] Імпорт з MEDoc / Soneta
- [ ] Локалізація (англійська)

---

## ❓ Питання

Маєте сумніви — створіть **Issue з міткою `question`** або напишіть в обговоренні.

**Дякуємо! 💛💙**
